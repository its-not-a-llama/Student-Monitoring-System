Python application for tracking and analysing student academic data, with 
automated underperformance detection and an interactive GUI.

Uses z-score analysis to flag students performing significantly below cohort 
average. Visualisations update dynamically via ipywidgets. SQLite backend 
keeps the setup lightweight and self-contained.

**Tools:** Python, SQLite, pandas, Matplotlib, ipywidgets
